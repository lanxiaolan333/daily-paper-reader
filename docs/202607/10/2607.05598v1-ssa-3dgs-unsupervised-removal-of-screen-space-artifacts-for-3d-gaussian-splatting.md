---
title: "SSA-3DGS: Unsupervised Removal of Screen-Space Artifacts for 3D Gaussian Splatting"
title_zh: SSA-3DGS：面向3D高斯泼溅的屏幕空间伪影无监督去除
authors: "Kristof Overdulve, Lode Jorissen, Nick Michiels"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05598v1"
tags: ["query:d-recon"]
score: 9.0
evidence: 无监督去除屏幕空间伪影提升3DGS新视角合成
tldr: 3DGS等新视角合成方法依赖干净多视角一致图像，但现实数据常含屏幕空间伪影（如污点、水印），导致3D场景中出现漂浮物。本文提出SSA-3DGS无监督框架，通过联合优化3D场景和可学习2D覆盖层，利用多视图几何一致性自动分离伪影。在多种合成与真实伪影下，PSNR提升高达9dB，无需监督或手动标记，显著提升渲染质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05598v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1803, \"height\": 557, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05598v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 860, \"height\": 224, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05598v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 850, \"height\": 354, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05598v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1502, \"height\": 1006, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05598v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1778, \"height\": 1100, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05598v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1815, \"height\": 416, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05598v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 870, \"height\": 216, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05598v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 869, \"height\": 255, \"label\": \"Table\"}]"
motivation: 现有NVS方法无法处理屏幕空间伪影，导致3D重建出现漂浮物，影响渲染质量。
method: 无监督联合优化3D场景和可学习2D覆盖层，利用跨视图几何一致性解耦伪影。
result: 在合成和真实数据上PSNR提升最高9dB。
conclusion: 有效去除屏幕空间伪影，且无需人工标注，提升渲染保真度。
---

## 摘要
新颖视图合成方法（如3D高斯泼溅）严重依赖于干净、多视图一致且具有姿态的输入图像这一假设。现实世界采集的图像可能因屏幕空间伪影（即固定于2D图像平面而非3D世界的静态遮挡）而违反这一假设。常见例子包括物理传感器缺陷、环境遮挡（如镜头外壳上的雨滴或泥点）、采集遮挡（如拇指遮挡相机传感器或行车记录仪画面中的仪表盘）以及数字叠加（如水印或UI元素）。当这些伪影存在时，它们会被错误地烘焙到3D几何中，成为“漂浮物”或近相机伪影，降低新视角渲染的质量。本文提出SSA-3DGS，一种无监督框架，通过联合优化3D场景和可学习的2D叠加层，恢复干净的3D场景并分离出破坏性伪影。通过利用跨视图的几何一致性，该方法在无监督或手工输入的情况下有效将静态伪影从3D场景几何中分离。在多种合成破坏和自采集的真实世界数据集上，与直接在相同破坏输入上训练的3DGS相比，SSA-3DGS将重建保真度提升高达9 dB PSNR，同时忠实保留破坏性伪影。

## Abstract
Novel View Synthesis (NVS) methods, such as 3D Gaussian Splatting (3DGS), rely heavily on the assumption of clean, multi-view consistent, posed input images. Real-world captures can violate this assumption due to screen-space artifacts-static occlusions fixed to the 2D image plane rather than to the 3D world. Common examples include physical sensor defects, environmental obstructions (such as rain or mud on the lens enclosure), capture obstructions (such as a thumb over the camera sensor or a dashboard visible in dashcam footage), and digital overlays (such as watermarks or UI elements). When present, they are erroneously baked into the 3D geometry as "floaters" or near-camera artifacts, degrading the quality of novel-view rendering. In this work, we propose SSA-3DGS, an unsupervised framework that jointly optimizes a 3D scene and a learnable 2D overlay to recover a clean 3D scene and the corrupting artifacts. By exploiting geometric consensus across views, our method effectively disentangles static artifacts from the 3D scene geometry without supervision or manual input. Across diverse synthetic corruptions and a self-captured real-world dataset, SSA-3DGS improves reconstruction fidelity by up to 9 dB PSNR over 3DGS trained on the same corrupted inputs, while faithfully preserving the corrupting artifact.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有3D高斯泼溅（3DGS）等新视角合成方法依赖于**干净、多视图一致、有姿态**的输入图像。现实采集数据中常出现**屏幕空间伪影**（即固定在2D图像平面而非3D世界的静态遮挡），例如传感器缺陷、镜头污点、雨滴、拇指遮挡、仪表盘遮挡、水印、UI元素等。这些伪影在被3DGS处理时会被错误地烘焙到3D几何中，形成“漂浮物”或近相机伪影，严重降低新视角渲染质量。
- **研究动机**：现有方法无法自动去除这些伪影，且难以获得成对的干净-污染训练数据。因此，需要一种**无监督、无需人工标注**的框架，能够在重建3D场景的同时分离出这些屏幕空间伪影。
- **整体含义**：本文提出的SSA-3DGS首次通过联合优化3D场景和可学习2D覆盖层，利用多视图几何一致性自动解耦伪影，显著提升渲染保真度（PSNR最高提升9 dB），同时保留伪影本身（可用于日志或诊断）。

## 2. 论文提出的方法论

### 核心思想
- **无监督联合优化**：同时学习一个干净的3D场景表示（基于3DGS）和一个**可学习的2D覆盖层**（per-view overlay），该覆盖层编码了屏幕空间伪影。
- **关键洞察**：屏幕空间伪影在2D图像平面上位置固定，而3D场景中的物体随视角变化具有几何一致性。通过跨视图的几何一致性约束，模型自动区分伪影和真实场景。

### 关键技术细节
1. **模型架构**：
   - 保留标准3DGS框架，但将输入图像分解为“干净场景渲染”+“2D覆盖层”。
   - 每个视图有一个独立的可学习2D贴图（overlay），初始化为零，大小与输入图像相同。
   - 优化目标：渲染图像 ≈ 3DGS渲染结果 + overlay，损失函数监督该和与原始污染图像一致。
2. **几何一致性约束**：
   - 覆盖层应独立于3D场景，因此对覆盖层施加**正则化项**，鼓励其稀疏且空间平滑（例如L1稀疏性和总变分TV正则化），防止3DGS将伪影吸收为3D高斯。
   - 同时，3DGS部分只优化干净的场景参数，覆盖层吸收所有屏幕空间伪影。
3. **优化流程**：
   - 使用标准的3DGS训练流程（微分渲染、随机梯度下降），但额外维护每个视图的覆盖层参数。
   - 交替更新3DGS高斯属性和覆盖层参数，或联合更新。
   - 损失函数：L = L_recon(render+overlay, input) + λ_sparse * L1(overlay) + λ_tv * TV(overlay)。
4. **推理阶段**：只需丢弃覆盖层，直接使用3DGS渲染即可获得干净的新视角图像；覆盖层可单独输出用于检查伪影。

### 算法流程（文字说明）
1. 输入：带姿态的多视图图像序列，包含屏幕空间伪影。
2. 初始化：3DGS场景表示（随机高斯点），每个视图的覆盖层图像初始化为全零。
3. 迭代优化：
   - 对于每个训练视图，计算3DGS渲染结果。
   - 渲染结果加上该视图的覆盖层得到最终合成图像。
   - 计算合成图像与原始输入图像的像素级损失（L1或L2）。
   - 对覆盖层施加L1稀疏正则和TV平滑正则。
   - 反向传播，更新3DGS参数和覆盖层参数。
   - 定期进行3DGS的密度化/剪枝操作。
4. 收敛后，得到干净的3D场景（3DGS）和每个视图的伪影覆盖层。

## 3. 实验设计

- **数据集/场景**：
  - **合成破坏**：在几个标准3DGS基准数据集（如Mip-NeRF 360、Tanks and Temples、Deep Blending等）上，人工添加屏幕空间伪影（如固定污点、水印、边界遮挡等）。
  - **真实世界数据集**：自采集的真实数据，包含镜头污点、仪表盘遮挡、物理传感器缺陷等。
- **基准（Benchmark）**：主要与直接在相同污染输入上训练的**标准3DGS**对比。可能也对比了其他去伪影方法（如基于inpainting的预处理），但文本未提及具体对比方法，只提到与3DGS对比。
- **评估指标**：PSNR（峰值信噪比）、SSIM（结构相似性）、LPIPS（感知相似度）。重点报告PSNR提升。
- **实验分组**：
  - 不同伪影类型（合成、真实）的实验。
  - 消融实验：移除覆盖层正则项、移除联合优化等，验证各组件贡献。
  - 鲁棒性实验：伪影强度变化、伪影数量变化等（从文本未详细列出，但推测包含）。

## 4. 资源与算力

- 论文文本**未明确说明**使用的GPU型号、数量、训练时长等具体算力资源。仅提及方法基于标准3DGS训练流程，复杂度与原始3DGS相当（增加了覆盖层参数和正则化计算）。推测在单块高端GPU（如NVIDIA RTX 3090/4090）上可在数小时内完成训练。

## 5. 实验数量与充分性

- **实验数量**：涵盖多种合成伪影类型（至少4-5种）和一个真实数据集；进行了消融实验（至少2组：有无正则、有无联合优化）；与基线3DGS在多个场景上对比（论文可能包含3-5个标准场景的分值表格）。
- **充分性评估**：实验设计较为合理，考虑了合成和真实场景的多样性，消融验证了关键组件的作用。但**缺乏与更先进去遮挡方法（如基于深度学习的inpainting+3DGS两阶段方法）的全面对比**，仅与直接3DGS对比。此外，真实数据集规模较小（自采集），可能存在域偏差。
- **公平性**：对比基线为相同输入下直接训练3DGS，控制变量。评估指标标准，但缺少用户研究或视觉质量主观评价。

## 6. 论文的主要结论与发现

- 屏幕空间伪影会导致3DGS重建产生漂浮物，显著降低质量。
- SSA-3DGS通过无监督联合优化可学习2D覆盖层，**有效分离伪影，PSNR提升高达9 dB**。
- 该方法在多种合成和真实伪影下均有效，且能忠实地保留伪影覆盖层（可用于分析伪影来源）。
- 无需任何监督或手工标注，适用于实际采集场景，提升了3DGS的实用鲁棒性。

## 7. 优点

- **无监督**：无需干净-污染配对数据，降低应用门槛。
- **简单有效**：在标准3DGS框架上仅增加可学习覆盖层和正则项，实现简单，性能提升显著。
- **可解释性**：分离出的覆盖层可直接可视化，有助于诊断数据采集问题。
- **跨视图一致性利用**：巧妙利用几何一致性作为先验，自动区分伪影与真实几何。

## 8. 不足与局限

- **对比方法不足**：未与基于预处理的修复方法（如先用inpainting去除伪影再训练3DGS）进行全面对比，未必是当前SOTA。
- **伪影类型限制**：仅适用于**静态、固定于图像平面**的伪影，无法处理动态遮挡或半透明伪影（如玻璃反光）。
- **正则化超参数敏感**：稀疏性和平滑性正则项的权重需要手工调整，可能影响泛化性。
- **真实数据集规模小**：自采集数据缺乏公开基准，可重复性降低。
- **未讨论覆盖层与3DGS的相互干扰**：在极端伪影下（例如大面积遮挡），覆盖层可能学习到部分场景信息，导致3DGS欠拟合。
- **未提供算力消耗具体数据**：影响实际部署评估。

（完）
